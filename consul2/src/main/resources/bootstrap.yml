spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        healthCheckPath: /health
        healthCheckInterval: 5s
        instance-id: consul-miya2
        service-name: app
        tags: test
        port: ${server.port}
        enabled: true
        hostname: localhost
  application:
    name: consul-miya
server:
  port: 8503